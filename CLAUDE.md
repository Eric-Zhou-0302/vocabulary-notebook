# Vocabulary Notebook

## 环境
- Python 虚拟环境：`.venv/`，所有 Python 操作必须在 venv 内执行
- 前端：React + Vite，`npm` 命令在 `frontend/` 目录下执行
- 本地 LLM：Ollama (gemma4:26b)，通过 REST API 调用

## 项目结构
- 后端：FastAPI，入口 `app.py`
- 前端：React SPA，目录 `frontend/`
- 数据：本地 JSON 文件存储
